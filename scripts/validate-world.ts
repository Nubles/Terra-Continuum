import fs from 'node:fs'; import type { World } from '../simulation/models/world.js'; const w:World=JSON.parse(fs.readFileSync('data/current-world.json','utf8'));if(w.grid.cells.length!==w.grid.width*w.grid.height)throw new Error('Grid dimensions do not match cell count');for(const cell of w.grid.cells)for(const n of [cell.elevation,cell.temperature,cell.moisture,cell.fertility,cell.water])if(!Number.isFinite(n)||n<0||n>1)throw new Error('Invalid cell metric');for(const s of w.species)if(s.population<0)throw new Error(`Negative population: ${s.id}`);console.log(`World valid: year ${w.meta.year}, ${w.species.length} species.`);
